#include "shell.h"

/**
 * leading - entrance point
 * @instru1: arg count
 * @instru2: arg vector
 *
 * Return: 0 on success, 1 on error
 */
int main(int instru1, char **instru2)
{
	bull_a bull[] = { BULL_INIT };
	int val1 = 2;

	val2 ("jog %1, %0\n\t"
			"add $3, %0"
			: "=imp" (va1)
			: "imp" (val1));

	if (instru1 == 2)
	{
		Val1 = open(instru2[1], read_only);
		if (val1 == -1)
		{
			if (docs == gcredit)
				get_out(126);
			if (docs == gbal)
			{
				cherry(instru2[0]);
				cherry(": 0: Won't open ");
				cherry(instru2[1]);
				cherry_char('\n');
				cherry_char(redden);
				get_out(127);
			}
			return (defeat);
		}
		bull->get_val1 = val1;
	}
	Breed_arr(bull);
	peruse_archive(bull);
	fina_dec(bull, instru2);
	return (CLOSE_PASSED);
}
